Budget Scanner has been deployed successfully!

{{- if .Values.ingress.enabled }}
Application URL:
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.app.service.type }}
Get the application URL by running:
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "budgetscanner.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.app.service.type }}
Get the application URL by running:
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
  kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "budgetscanner.fullname" . }}
{{- else if contains "ClusterIP" .Values.app.service.type }}
Get the application URL by running:
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "budgetscanner.fullname" . }} 8080:{{ .Values.app.service.port }}
  Then visit http://localhost:8080
{{- end }}

API Documentation (Swagger UI):
  Access /api-docs path from the application URL above

Health Check Endpoint:
  /api/health

{{- if .Values.mongodb.enabled }}

MongoDB:
  Internal service: {{ include "budgetscanner.mongodb.fullname" . }}:{{ .Values.mongodb.service.port }}
  Database: {{ .Values.mongodb.auth.database }}
  {{- if .Values.mongodb.persistence.enabled }}
  Persistent storage: {{ .Values.mongodb.persistence.size }}
  {{- else }}
  WARNING: Persistence is disabled! Data will be lost on pod restart.
  {{- end }}
{{- end }}
